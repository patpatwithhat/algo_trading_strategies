/**
 * Base class for custom errors.
 */
export declare class CustomError extends Error {
    constructor(message: string);
}
/**
 * Base class for a custom error which wraps another error.
 */
export declare class WrappedError<T = Error> extends CustomError {
    readonly originalError: T;
    constructor(message: string, originalError: T);
}
export declare class ConfigError extends CustomError {
}
