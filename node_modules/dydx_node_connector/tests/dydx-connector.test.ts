import assert = require("assert")
import Connector from "../src/dydx-connector"


describe('Setup', () => {
    it('should work', () => {
        expect(true).toBeTruthy()
    })


    it('Find required keys', () => {
      assert.equal(Connector.checkGeneralKeysPresent(), true)
    })
})


