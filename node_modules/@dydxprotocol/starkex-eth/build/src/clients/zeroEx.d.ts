import { ZeroExSwapResponse } from '../types';
export declare function getZeroExSwapQuote({ sellAmount, sellToken, buyTokenAddress, slippageFraction, networkId, }: {
    sellAmount: string;
    sellToken: string;
    buyTokenAddress: string;
    slippageFraction?: string;
    networkId: number;
}): Promise<ZeroExSwapResponse>;
export declare function validateSlippage(slippage?: string): void;
